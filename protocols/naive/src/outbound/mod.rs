//! `Outbound` для обоих транспортов naive: HTTP/2 и HTTP/3.
//!
//! Реализации-близнецы: один и тот же `CONNECT`, одно и то же дополнение —
//! отличается только то, как устроен нижний поток. Общий код уже вынесен в
//! [`crate::connect`] и [`crate::padding`], и здесь остаётся только то, что
//! у HTTP/2 и HTTP/3 по-настоящему разное: как открыть соединение и как
//! завести новый поток внутри него.

pub mod h2;
pub mod h3;

pub use h2::NaiveHttp2Outbound;
pub use h3::NaiveHttp3Outbound;
