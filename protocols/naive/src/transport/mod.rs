//! Соединение с сервером: HTTP/2 поверх TCP+TLS, HTTP/3 поверх QUIC.
//!
//! Ни тот, ни другой не открывает сокет сам — оба берут его у
//! [`penguin_proto::dialer::Dialer`] (`AGENTS.md`, §3): TUN перехватывает
//! весь трафик машины, и сокет, открытый в обход этого правила, отправил бы
//! пакеты рукопожатия в собственный, ещё не поднятый тоннель.

pub mod h2;
pub mod h3;
