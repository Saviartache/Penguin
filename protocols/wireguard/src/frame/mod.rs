//! Разбор и сборка байт WireGuard: сообщения рукопожатия и пакеты данных.
//!
//! Здесь нет ни сокетов, ни `tokio`, ни ключей — чистая раскладка байт,
//! которая проверяется без сети. Что значат эти байты (шифрование, MAC,
//! проверка подлинности) — решает `crate::crypto::handshake` и
//! `crate::crypto::session`, а не этот модуль.

pub mod initiation;
pub mod response;
pub mod transport;
