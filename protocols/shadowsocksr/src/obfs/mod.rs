//! Надстройка `obfs`: то, что видно снаружи ещё до расшифровки.
//!
//! ```text
//!  method  имя надстройки в настройках
//!  state   состояние на одно соединение, выбор варианта
//! ```
//!
//! Обфускация не шифрует — внутри и так шифр протокола (`crypto`), — она
//! только меняет вид пакета снаружи. `plain` не делает ничего, `http_simple`
//! прикидывается запросом `GET`. Подробности и то, чего здесь нет
//! (`tls1.2_ticket_auth`, `http_post`, `random_head`) — в документе крейта.

pub mod http_simple;
pub mod method;
pub(crate) mod state;

pub use method::ObfsMethod;
