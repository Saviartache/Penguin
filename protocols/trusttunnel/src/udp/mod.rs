//! UDP поверх единого потока `CONNECT _udp2` (`PROTOCOL.md`, §6).
//!
//! `frame` — чистый разбор и сборка кадров, без сети. `session` — то, что
//! держит сам поток и раскладывает пришедшие кадры по сессиям приложений.

pub mod frame;
pub mod session;
