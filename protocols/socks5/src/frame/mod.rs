//! Разбор и сборка байт SOCKS5.
//!
//! Запись адреса здесь не лежит: ею пользуются Trojan, Shadowsocks и
//! ещё четверо, и живёт она в `penguin_transport::addr::socks`.
//!
//! Здесь нет ни сокетов, ни `tokio`: чистые функции над срезами, которые
//! проверяются без сети и без ожидания. Всё, что умеет ждать, живёт в
//! [`crate::handshake`] и [`crate::datagram`].

pub mod udp;
