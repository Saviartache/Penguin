# Что не должно попасть в контекст сборки. `stage.sh` кладёт этот файл рядом
# с `Dockerfile` под именем `.dockerignore`.
#
# Главное здесь — `target/`. Он появляется, стоит один раз собрать контекст
# на месте, и весит десятки-сотни мегабайт чужих артефактов: в образе они
# бесполезны (сборка идёт своя, в другом окружении), а в контексте — это
# минуты на каждый `docker build` и столько же на каждую выгрузку.
target/
**/target/

# Ключ и пароли сервера. В образ они не входят никогда: `compose.yml`
# подсовывает их томом, потому что образ пересобирается, а ключ — нет.
pingwin.toml
.env

.git/
.DS_Store
