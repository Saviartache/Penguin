//! Разбор IP-заголовков до сборки сокета.
//!
//! Два разных повода трогать пакеты руками, минуя smoltcp.
//!
//! **TCP.** Решение о судьбе соединения принимается по первому пакету, до
//! того как сокет вообще создан: чтобы завести его, надо знать, кому и куда.
//!
//! **UDP.** Через smoltcp он не идёт вовсе. Состояния у UDP нет, а заводить
//! сокет на каждую пару адресов пришлось бы сотнями — один сокет приложения
//! шлёт куда угодно. Разобрать заголовок и собрать ответный пакет дешевле.

pub mod checksum;
pub mod parse;

pub use parse::{PROTO_TCP, PROTO_UDP, Packet, parse};
