# Прокси для проверки `http`. Пароля нет намеренно: проверяется CONNECT,
# а не разбор `Proxy-Authorization`, — у него свой тест на записанных байтах.
User nobody
Group nogroup
Port 8888
Timeout 600
MaxClients 20
Allow 0.0.0.0/0
ConnectPort 80
ConnectPort 443
